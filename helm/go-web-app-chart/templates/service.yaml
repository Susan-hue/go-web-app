apiVersion: v1
kind: Service
metadata:
  name: {{.Values.metadata.name }}
  labels:
    app: {{ .Values.metadata.labels.app }}
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: {{ .Values.metadata.labels.app }}
  type: ClusterIP